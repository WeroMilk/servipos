/** Logo SERVIPARTZ vectorial. Respeta `base` de Vite. */
export const BRAND_LOGO_URL = `${import.meta.env.BASE_URL}servipartz-logo-v2.svg`;

/** `srcset` explícito para pantallas retina (SVG escala nítido en cualquier densidad). */
export const BRAND_LOGO_SRCSET = `${BRAND_LOGO_URL} 1x, ${BRAND_LOGO_URL} 2x`;
